export enum EControls{
    left = 'left',
    right = 'right',
    down = 'down',
    rotate = "rotate",
    up = 'up'
}
export enum EColors{
    red ='red',
    orange = 'orange',
    // gray= 'gray',
    green = 'green'
}

export enum EGameStatus{
    pending,
    palying,
    pasuse,
    over,
}